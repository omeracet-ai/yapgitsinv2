<?xml version="1.0" encoding="utf-8"?>
<!--
  Yapgitsin Admin Panel — Next.js standalone on IIS + iisnode (Phase 166)
  Deployed to: D:\admin on Plesk Windows host (yapgitsin.tr/admin)
  Entry point: server.js (Next.js standalone server)
  Requires: iisnode module installed (%programfiles%\iisnode\iisnode.dll)
-->
<configuration>
  <system.webServer>

    <!-- iisnode aktif. Plesk -> Node.js: Application Root=httpdocs/admin, Startup File=server.js, NODE_ENV=production. iisnode modülü Plesk Node.js extension'ı kuruluysa devrede. -->
    <handlers>
      <add name="iisnode" path="server.js" verb="*" modules="iisnode" />
    </handlers>

    <!-- iisnode bind edilmezse statik placeholder fallback için default document -->
    <defaultDocument>
      <files>
        <clear />
        <add value="index.html" />
      </files>
    </defaultDocument>

    <!-- URL Rewrite: forward all incoming requests to the Next.js standalone entry -->
    <rewrite>
      <rules>
        <!-- Skip rewrite for iisnode debugger UI -->
        <rule name="NodeInspector" patternSyntax="ECMAScript" stopProcessing="true">
          <match url="^server.js\/debug[\/]?" />
        </rule>

        <!-- Pass-through Next.js static assets directly via IIS for performance -->
        <!-- NextStatic / PublicAssets intercepts REMOVED: Next.js standalone serves
             /_next/static/* from disk path .next/static/* (dot prefix). IIS bypass
             404s on the path mismatch. Letting server.js handle everything is the
             correct + safest pattern for Next.js standalone under iisnode. -->

        <!-- All other requests -> server.js -->
        <rule name="DynamicContent">
          <match url="/*" />
          <action type="Rewrite" url="server.js" />
        </rule>
      </rules>
    </rewrite>

    <!-- iisnode runtime config Plesk Node.js extension tarafından yönetiliyor (node binary yolu, logging vs). Buraya iisnode runtime elementi EKLEME — nodeProcessCommandLine override'ı 500.1000'e yol açar. -->

    <!-- Security: hide sensitive files -->
    <security>
      <requestFiltering>
        <!-- <clear /> drops segments inherited from the parent (/httpdocs/web.config),
             preventing "duplicate collection entry ... node_modules" 500.19 errors. -->
        <hiddenSegments>
          <clear />
          <add segment="web.config" />
          <add segment="node_modules" />
          <add segment="iisnode" />
          <add segment=".env" />
          <add segment=".env.production" />
          <!-- .next REMOVED from hiddenSegments: Next.js standalone's static assets live
               under .next/static/* and must be readable by IIS via server.js routing.
               Direct browser access to .next/server/* / .next/cache/* is still impossible
               because the DynamicContent rule routes everything to server.js, which only
               serves /_next/static/* publicly. -->
        </hiddenSegments>
        <fileExtensions>
          <add fileExtension=".env" allowed="false" />
        </fileExtensions>
      </requestFiltering>
    </security>

    <httpErrors errorMode="Detailed" />

  </system.webServer>
</configuration>
