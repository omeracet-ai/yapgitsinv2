# PENDING DDL — Production MariaDB Migrations Awaiting Run

DNS to ftp.yapgitsin.tr çözümlenemiyor + port 3306 dış erişim bloklu (geçici).
Önceki Phase 17–24 DB-Agent'ları aynı yolu kullanıp başarılı çalıştı.
Bağlantı düzeldiğinde aşağıdaki script'leri sırayla çalıştır.

Tüm script'ler **idempotent** — tekrar çalıştırılırsa skip eder, zarar vermez.

---

## Phase 22 — 2FA TOTP (commit 49c7c882)

cd nestjs-backend
node add-2fa-columns.js

Beklenen log:
- [ok] users.twoFactorEnabled added/skipped
- [ok] users.twoFactorSecret added/skipped

Etki:
- /auth/2fa/setup ve /auth/2fa/verify endpoint'leri çalışır hale gelir.
- Authenticator app ile iki adımlı doğrulama aktif olur.

---

## Phase 23 — User Block & Report (commit c6332cf2)

cd nestjs-backend
node add-user-blocks-tables.js

Beklenen log:
- [ok] user_blocks created/skipped
- [ok] user_reports created/skipped

Etki:
- /users/:id/block ve /users/:id/report endpoint'leri çalışır hale gelir.
- Peer-level engelleme ve şikayet akışı aktif olur.

---

## Phase 24 — Password Reset (commit 9c9da5ec)

cd nestjs-backend
node add-password-reset-table.js

Beklenen log:
- [ok] password_reset_tokens created/skipped

Etki:
- /auth/forgot-password ve /auth/reset-password endpoint'leri çalışır hale gelir.
- Hash'lenmiş token tabanlı şifre sıfırlama akışı aktif olur.

---

## Phase 25 — Email Verification (commit b60a052a)

cd nestjs-backend
node add-email-verify-schema.js

Beklenen log:
- [ok] users.emailVerified added
- [ok] email_verification_tokens created/skipped

Etki:
- /auth/verify-email/request ve /auth/verify-email/confirm endpoint'leri çalışır hale gelir.
- ProfileScreen email rozet "Doğrula" butonu fonksiyonel olur.

---

## Phase 26 — Admin Audit Log (commit pending)

cd nestjs-backend
node add-admin-audit-table.js

Beklenen log:
- [ok] admin_audit_logs created/skipped
- [ok] idx_admin_audit_admin created/skipped
- [ok] idx_admin_audit_target created/skipped

Etki:
- GET /admin/audit-log endpoint'i çalışır hale gelir.
- Admin verify / featured / report-update aksiyonları artık audit'e yazılır.

Doğrulama:
SHOW TABLES LIKE 'admin_audit_logs'  → 1 satır beklenir.

---

## Hızlı Doğrulama (script çalıştıktan sonra)

cd nestjs-backend
node -e "require('dotenv').config({path:'.env.production'});require('dotenv').config({path:'.env.production.local',override:true});const m=require('mysql2/promise');(async()=>{const c=await m.createConnection({host:process.env.DB_HOST,user:process.env.DB_USERNAME,password:process.env.DB_PASSWORD,database:process.env.DB_NAME});const [r1]=await c.query(\"SHOW COLUMNS FROM users WHERE Field='emailVerified'\");const [r2]=await c.query(\"SHOW TABLES LIKE 'email_verification_tokens'\");console.log('emailVerified:',r1.length>0,'| table:',r2.length>0);await c.end();})();"

Beklenen: `emailVerified: true | table: true`

---

## Bağlantı Test Komutu

cd nestjs-backend
node test-mysql.js

veya direkt IP ile:
node test-mysql.js 93.89.224.84

Bağlantı OK ise yukarıdaki migration script'lerini çalıştır.
