# Production build (used by `npm run build` when NODE_ENV=production)
# Backend URL must be reachable from the build machine.
NEXT_PUBLIC_API_URL=https://api.yapgitsin.tr
NEXT_PUBLIC_SITE_URL=https://yapgitsin.tr
